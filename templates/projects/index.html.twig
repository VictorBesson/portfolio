{% extends 'base.html.twig' %}

{% block title %}Projets
{% endblock %}

{% block body %}


	<div class="container">

		<div class="row col-15">
			{% for project in projects %}
				<div class="col">
					<div class="card shadow p-3 mb-4 bg-dark rounded">

						<div class="card-body bg-dark">
							<h1>{{ project.name }}</h1>
							<p class="card-text">{{ project.description }}</p>
							<div class="d-flex justify-content-between align-items-center">
								<div class="btn-group">
									{%if project.linkProject %}
										<button onclick="location.href='{{ project.linkProject }}'" type="button" class="btn btn-sm btn-outline-secondary">Voir</button>
									{% endif %}
									<button onclick="location.href='{{ project.link }}'" type="button" class="btn btn-sm btn-outline-secondary">Github</button>
								</div>
							</div>
						</div>
					</div>
				</div>
			{% endfor %}
		</div>
	{% endblock %}
